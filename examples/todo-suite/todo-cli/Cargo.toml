[package]
name = "todo-cli"
version = "0.1.0"
edition = "2021"
description = "Command-line interface for the CADI demo todo list"

[[bin]]
name = "todo"
path = "src/main.rs"

[dependencies]
todo-core = { path = "../todo-core" }
clap = { version = "4.5", features = ["derive"] }
serde_json = "1.0"
