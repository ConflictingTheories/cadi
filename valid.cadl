@interface VideoCodec {
    encode(frame: ImageData, quality: u8) -> Blob
    
    @contract {
        codec: "h264"
        profile: "baseline"
    }

    @effects {
        concurrency: "thread_safe"
    }
}
